{
  "name": "name",
  "version": "1.0.0",
  "main": "index.ts",
  "scripts": {
    "start:dev": "npx nodemon ./src/index.ts",
    "start:prod": "rm -rf ./dist/ && npx tsc --rootDir src --outDir dist && node ./dist/index.js",
    "dev": "npx nodemon ./src/index.ts",
    "build": "rm -rf ./dist/ && npx tsc --rootDir src --outDir dist",
    "lint": "npx eslint src/ --ext ts"
  },
  "dependencies": {
    "dotenv": "^16.0.1",
    "ts-node": "^10.8.1",
    "typescript": "^4.7.3"
  },
  "devDependencies": {
    "@types/node": "^17.0.36"
  }
}
